// to create an array, you can use the array literal syntax:
// example: const array = [1, 2, 3];

// to create an array, you can use the array constructor:
// example: const array = new Array(1, 2, 3);

// to retrieve an element from an array, you can use the index:
// example: const array = [1, 2, 3];
// console.log(array[0]); // returns 1

// to retrieve the length of an array, you can use the length property:
// example: const array = [1, 2, 3];
// console.log(array.length); // returns 3

// to change an element in an array, you can use the index:
// example: const array = [1, 2, 3];
// array[0] = 4;
// console.log(array); // returns [4, 2, 3]
